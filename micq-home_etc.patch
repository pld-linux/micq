diff -Nur micq-0.4.2-orig/CHANGELOG micq-0.4.2/CHANGELOG
--- micq-0.4.2-orig/CHANGELOG	Mon Sep 13 03:42:33 1999
+++ micq-0.4.2/CHANGELOG	Sat Nov 27 18:43:21 1999
@@ -1,3 +1,6 @@
+Nov 27, 1999
+changed to use ~/etc/micqrc (but still can read old ~/.micqrc)
+
 Sept 12, 1999
 Improved tabbed nick names
 added more info to the update commad
diff -Nur micq-0.4.2-orig/README micq-0.4.2/README
--- micq-0.4.2-orig/README	Mon Sep 13 03:42:33 1999
+++ micq-0.4.2/README	Sat Nov 27 18:43:46 1999
@@ -53,7 +53,7 @@
 Ok to run it type micq
 It will ask for your uin and password.  Two users are in the default contact 
 list. The two users are two different uin's I use.  You can add more by editing
-the ~/.micqrc file.
+the ~/etc/micqrc file.
 Ok if you successfully login then you can type 
 msg uin/message
 -or-
@@ -71,7 +71,7 @@
 
 Passwords
 
-If you don't want your password stored on disk edit the ~/.micqrc file and 
+If you don't want your password stored on disk edit the ~/etc/micqrc file and 
 remove the password line and you will be prompted to type it everytime Micq 
 starts.
 
diff -Nur micq-0.4.2-orig/file_util.c micq-0.4.2/file_util.c
--- micq-0.4.2-orig/file_util.c	Mon Sep 13 03:42:33 1999
+++ micq-0.4.2/file_util.c	Sat Nov 27 18:42:14 1999
@@ -33,11 +33,13 @@
               strncpy( b,strtok(NULL,"\n"), 450 );                  \
           }                                                          
 
-static char rcfile[256];
+char *rcfile;
 
 static void Initalize_RC_File( void )
 {
    FD_T rcf;
+   char *path;
+   char *home;
    char passwd2[ sizeof(passwd) ];
    strcpy( server, "icq1.mirabilis.com" );
    remote_port = 4000;
@@ -134,6 +136,19 @@
    Current_Status = STATUS_ONLINE;
    
    rcf = open( rcfile, O_RDWR | O_CREAT, S_IRUSR | S_IWUSR);
+   if (( rcf == -1 ) && ( errno == ENOENT )) {
+      home = getenv( "HOME" );
+      path = malloc( strlen( home ) + 14 );
+      strcpy( path, home );
+      if ( path[ strlen( path ) - 1 ] != '/' ) 
+         strcat( path, "/" );
+      strcat( path, "etc" );
+      if ( mkdir(path, 0700) == -1 ) {
+         perror( "Error creating ~/etc directory" );
+         exit (1 );
+      }
+      rcf = open( rcfile, O_RDWR | O_CREAT, S_IRUSR | S_IWUSR);
+   }
    if ( rcf == -1 )
    {
       perror( "Error creating config file " );
@@ -609,14 +624,22 @@
 #ifdef __amigaos__
    path = "PROGDIR:";
 #endif
-
+   rcfile = malloc (strlen (path) + 14 );
    strcpy( rcfile, path );
-   strcat( rcfile, ".micqrc" );
-   rcf = open( rcfile, O_RDONLY );
+   strcat( rcfile, "etc/micqrc" );
+   rcf = open ( rcfile, O_RDONLY );
+   if ( rcf == -1 )
+   {
+      strcpy( rcfile, path );
+      strcat( rcfile, ".micqrc" );
+      rcf = open( rcfile, O_RDONLY );
+   }
    if ( rcf == -1 )
    {
       if ( errno == ENOENT ) /* file not found */
       {
+         strcpy( rcfile, path );
+         strcat( rcfile, "etc/micqrc" );
          Initalize_RC_File();
       }
       else
